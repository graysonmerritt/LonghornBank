@model IEnumerable<Team4_Final_Project.Models.Account>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <br /><a asp-action="Create" class="btn btn-primary">Apply for a Checkings, Saving, or IRA</a> |
    <a asp-controller="StockPortfolios" asp-action="Create"class="btn btn-primary">Apply for a Stock Portfolio</a> |
    <a asp-controller="Transactions" asp-action="Transfer" class="btn btn-primary">Transfer</a> |
    <a asp-controller="Transactions" asp-action="Deposit" class="btn btn-primary">Make a Deposit</a> |
    <a asp-controller="Transactions" asp-action="Withdrawal" class="btn btn-primary">Make a Withdrawal</a>
    
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HiddenAccountNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nickname)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Balance)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.HiddenAccountNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nickname)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.Type)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Balance)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.AccountID">Edit</a> |
                    @if (item.isActive || (User.Identity.IsAuthenticated && User.IsInRole("Admin")))
                    {
                        <a asp-action="Details" asp-route-id="@item.AccountID">Details</a> <text>| </text>
                        <a asp-action="Delete" asp-route-id="@item.AccountID">Delete</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
